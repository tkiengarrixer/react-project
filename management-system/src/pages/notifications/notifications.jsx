import { Typography } from "antd"

function Notifications() {
    return <div>
        <Typography.Title level={4} style={{ fontWeight: "bold" }}>Thông báo</Typography.Title>
    </div>
}

export default Notifications